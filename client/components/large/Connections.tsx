import GeneralCard from "./GeneralCard";

interface Props {}

const NewConnection = (props: Props) => {
	return (
		<GeneralCard
			firstTitle="Spojení"
			height="h-full"
			firstChildren={<></>}
		/>
	);
};

export default NewConnection;
